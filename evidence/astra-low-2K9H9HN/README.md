# Astra-low visible run: 2K9H9HN

Confirmed Ante8 clear, continued in endless mode, then lost at Ante11.
Peak single-hand score:1,239,454. Recorded totals:384 decisions,357 coach requests,
3611.146 active seconds. These include the continued portions of the same game;
coach requests include rejected/interrupted responses. `won=true` and
`ante_8_cleared=true` record the earlier milestone, while `status=lost` records
how endless play ended.

Real Red Deck / White Stake / all-unlocked, visible fast playback, gpt-6-astra low
through Codex ChatGPT login at standard credits. No direct model API or premium
Fast mode. Astra selected every meaningful game action; cashouts were automatic.

This was a supervised development run with adapter fixes, prompt clarification,
reviewed continuations and explicitly extended limits. It is not a clean
unattended benchmark or a win-rate estimate. Recovery never replayed an uncertain
mutation. The initial slow visible attempt was reset at the user's request and
is separately labeled; it is not part of the completed game.

`result.json`/`manifest.json` describe the final endless segment.
`ante-8-result.json`/`ante-8-manifest.json` retain the initial clear.
`segments.json` orders every original trace segment and records SHA256 hashes of
its uncompressed source files. `segments/00` through `06` contain complete
compressed public traces, original manifests and results. The interrupted slow
attempt is preserved separately. No trajectory was edited to hide errors.

The first continuation reconciled Certificate's53-card deck with a stale52-card
area capacity. The next corrected an unsupported Hermit buy/use proposal.
Later continuations applied technical prompt changes, extended explicit call
limits, and enabled endless mode. One delayed Arcana draw was reconciled from
public observations. The chronological segments retain those boundaries.
