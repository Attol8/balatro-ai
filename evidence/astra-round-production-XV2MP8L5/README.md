# Astra low, round-production advice — XV2MP8L5

Completed 8 September 2026. GPT-6 Astra, low reasoning effort, Red Deck / White
Stake, all unlocked, real visible Balatro client, endless mode. The supervisor
reused the already-running game server; the supplied headless launch command was
only a fallback and does not make this a headless run. No screen recording was
made for this game. AI Watch followed it throughout.

**Ante 8 cleared; lost at Ante 12; peak hand 326,543,967 chips.** The Arm ended
the game with 19,198,174 of 600,000,000 after all five hands and all three
discards. Final phase `GAME_OVER`; reason `endless_game_over`. `won: true`
records the base-game win, while `status: lost` records the endless loss.

## Policy and provenance

Implementation: `9395d7a`, round-production and
copier-event advice, DNA singleton candidate and strategy corrections. This
commit was initially named `ae3e3b2` before local history was rewritten during
concurrent benchmark documentation work; the implementation is the same.
No strategy or gameplay code changed during this run. The raw manifest has no
policy-revision field and is archived unchanged. This new-policy game is a
separate benchmark row, not pooled with the earlier Astra games.

Seed XV2MP8L5 was generated by omitting `--seed`; no earlier local run/evidence
manifest used it. The model never received the seed. The previous finished game
was returned to the menu before launch; no human gameplay intervention occurred
after the new game started. No external video research was supplied during play.

Original command (from the repository virtual environment):

```sh
balatro supervise --endless --model gpt-6-astra \
  --output runs/astra-round-production-20260908 \
  --server-command 'BALATROBOT_ALL_UNLOCKED=1 uvx balatrobot serve --fast --headless --logs-path runs/logs'
```

One segment, **zero restarts**. Started 2026-09-07 23:17:09 UTC and ended
2026-09-08 01:13:07 UTC (both 8 September in local British Summer Time).
6957.258 seconds, 421 model calls, 515 decisions; below unchanged limits of
7200 seconds, 450 calls and 750 actions. The supervisor stopped because the
game finished, not because a budget or restart cap expired.

48 chained follow-up actions, 14 forced actions, zero rejected replies,
six recovered model timeouts and one recovered game-reply timeout. The
supervisor records and cumulative result are preserved at the root; the raw
segment files are under `segments/00/`. `segments.json` records SHA-256 hashes
of their uncompressed bytes. Trajectory gzip is deterministic (`mtime=0`).

## What the trace supports

The player developed Fibonacci/Hack/Glass scoring, duplicated Hanging Chad
through Invisible Joker, and later added Brainstorm in an Ante 11 shop. It used
two ante-reducing vouchers to buy further rounds. Final Jokers: Hanging Chad,
Hack, Fibonacci, Hanging Chad, Brainstorm.

At Ante 12 Small, on its final hand, it used three Glass 5s plus an ordinary 3
and Glass 3 for a Full House while holding three Steel cards. Chariot and two
adjacent Joker reorders set up Brainstorm copying Hack. The existing reorder
tool estimated 326,543,967 in the chosen order versus 74,338,992 in the prior
order; the chosen score matched the observed hand exactly. The prior-order
score is an estimate, not a played counterfactual. Two Glass cards disappeared
after this hand. The later Arm fight could not reproduce sufficient scoring:
its strongest hand was 11,191,972, and its final pair of 8s scored 161,374.

New round-production context appeared in **147** model requests: 73 with an
approximate current finish, 65 prioritizing survival without one, and nine
last-hand warnings. Sixteen exposed held Gold; none exposed Purple or Blue
opportunities. No supported production-copier targets occurred, so
`copier_timing` was empty throughout. Brainstorm's successful scoring reorder
used existing numerical support and does not validate the new copier-event
path. Offline regression tests cover those unexercised mechanics.

One fresh seed does not establish improvement. This run demonstrates a complete
uninterrupted game with the new advice and a correct natural stop; matched-seed
comparison and more consistent late-game scaling remain unmeasured.

[Review and implementation context](../../docs/high-score-video-review.md).

## Verification

The implementation passed 139 focused regression tests. All 59 publication tests passed, covering
benchmark tables, trace loading and figures; regenerated tables and SVGs match a
second output directory byte for byte. All three archived segment hashes match
the original files, and the cumulative result matches the final transition.
